// Generated by CoffeeScript 1.8.0
(function() {
  var collectionUsers, modelUser, viewAuth;

  window.auth = false;

  window.App = {
    views: {},
    models: {},
    collections: {}
  };

  App.models.user = Backbone.Model.extend({
    defaults: {
      id: _userId,
      sessionId: _sessionId,
      login: '',
      urlRoot: '/user'
    }
  });

  App.views.auth = Backbone.View.extend({
    el: $('#welcome-form'),
    events: {
      'submit': 'auth'
    },
    auth: function(event) {
      var login;
      event.preventDefault();
      login = $('input[name="login"]').val();
      return window.socket.connection.emit('test', {
        login: login
      });
    }
  });

  modelUser = new App.models.user;

  viewAuth = new App.views.auth({
    model: modelUser
  });

  App.views.users = Backbone.View.extend({
    model: modelUser
  });

  App.collections.users = Backbone.Collection.extend({
    model: App.models.user
  });

  collectionUsers = new App.collections.users;

}).call(this);
